{
  "name": "mdx-ts-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for mdx-ts: a type checker for MDX (CLI, language server, VS Code extension)",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.26.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter mdx-ts run test"
  }
}
